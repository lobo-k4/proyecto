const Vehicle = require ("../../models/entities/Vehicle")

// Crear una instancia de Vehicle
const myVehicle = new Vehicle("Toyota", "Corolla");
// Llamar al método getDetails()
myVehicle.getDetails();
